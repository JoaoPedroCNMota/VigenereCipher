package VigenereCipher;

public class VigenereCrack {

    public String attack(String encryptedMessage){

        for (int i = 0; i < encryptedMessage.length(); i++) {

            if (encryptedMessage.charAt(i) != 32){ // 32 = espaco, ASCII

            }

        }

        return null;
    }

}
